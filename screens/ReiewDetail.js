import React from "react";
import { StyleSheet, View, Text } from "react-native";

export default function ReiewDetail() {
    return (
        <View style={styles.container}>
            <Text>Review Detail Screen</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        padding: 24,
    },
});
